---
name: Trivy SCA

'on':
  workflow_call:
    inputs:
      app_dir:
        type: string
        required: false
        default: "."
      severity:
        type: string
        required: false
        default: "HIGH,CRITICAL"
      ignore_unfixed:
        type: string
        required: false
        default: "true"
      fail_on_findings:
        type: string
        required: false
        default: "true"

permissions:
  contents: read
  security-events: write

jobs:
  sca:
    name: Trivy SCA
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Escaneo SARIF (tu paso original) – usar scan-ref en lugar de working-directory
      - name: Trivy FS scan (SARIF)
        uses: aquasecurity/trivy-action@0.24.0
        with:
          scan-type: fs
          scanners: vuln
          format: sarif
          output: trivy.sarif
          scan-ref: "${{ inputs.app_dir }}"
          severity: "${{ inputs.severity }}"
          ignore-unfixed: "${{ inputs.ignore_unfixed }}"

      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy.sarif

      # Instalar Trivy CLI para sacar JSON y construir el Job Summary
      - name: Setup Trivy (CLI)
        uses: aquasecurity/setup-trivy@v0.2.1
        with:
          version: v0.56.1
          cache: true
          path: $HOME/.local/bin

      - name: Trivy JSON (for summary)
        run: |
          trivy fs "${{ inputs.app_dir }}" \
            --scanners vuln \
            --format json \
            --output trivy.json \
            --severity "${{ inputs.severity }}" \
            --ignore-unfixed=${{ inputs.ignore_unfixed }} \
            --exit-code 0

      - name: Build Job Summary
        run: |
          if [ -f trivy.json ]; then
            TOTAL=$(jq '[.Results[]?.Vulnerabilities[]?] | length' trivy.json 2>/dev/null || echo 0)
            H=$(jq '[.Results[]?.Vulnerabilities[]?|select(.Severity=="HIGH")] | length' trivy.json 2>/dev/null || echo 0)
            C=$(jq '[.Results[]?.Vulnerabilities[]?|select(.Severity=="CRITICAL")] | length' trivy.json 2>/dev/null || echo 0)
          else
            TOTAL=0; H=0; C=0
          fi

          STATUS="🟢 Sin hallazgos en ${{ inputs.severity }}"
          if [ "$TOTAL" -gt 0 ]; then STATUS="🟠 $TOTAL hallazgos (${{ inputs.severity }})"; fi
          if [ "$C" -gt 0 ]; then STATUS="🔴 $TOTAL hallazgos (${{ inputs.severity }})"; fi

          {
            echo "## 🔍 Trivy SCA (Dependencies)"
            echo ""
            echo "- **Severidades**: \`${{ inputs.severity }}\`  •  **Ignore unfixed**: \`${{ inputs.ignore_unfixed }}\`"
            echo ""
            echo "| Estado | Total | HIGH | CRITICAL |"
            echo "|:--:|--:|--:|--:|"
            echo "| $STATUS | $TOTAL | $H | $C |"
            echo ""
            echo "> El reporte SARIF fue subido a **Code scanning alerts** (pestaña *Security*)."
          } >> "$GITHUB_STEP_SUMMARY"

      # Mantén tu lógica de fallo exactamente igual
      - name: Fail on findings
        if: ${{ inputs.fail_on_findings == 'true' }}
        run: |
          COUNT=$(jq '[.runs[].results] | map(length) | add' trivy.sarif)
          test "${COUNT:-0}" -eq 0
